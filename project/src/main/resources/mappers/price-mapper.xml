<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="goott.spring.project1.PriceMapper">
	 <select id="select_price" resultType="PriceVO">
	 	select * from ${price} 
	 	where ${priceId}
	 </select>
	 <!-- 뺄지말지 고민중 -->
</mapper>









